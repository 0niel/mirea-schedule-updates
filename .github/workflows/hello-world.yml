name: hello-world

on:
  schedule:
    - cron: '10 * * * *'
  workflow_dispatch: 

  
jobs:
  hello-world-job:
    name: hello-world-job
    runs-on: ubuntu-latest
    steps:
      - name: Echo hello world every 10 minutes
        run: echo "Hello world!"
      - name: Directory
        run: pwd
      - name: print of directory
        run: echo ${{ github.workspace }}
      - name: Dotnet run
        run: dotnet run ./MireaScheduleUpdates/MireaScheduleUpdates.csproj
    